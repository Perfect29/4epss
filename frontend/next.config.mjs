/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // можешь добавить другие настройки при необходимости
    // например:
    // images: { domains: ["example.com"] },
    // experimental: { typedRoutes: true },
  };
  
  export default nextConfig;
  